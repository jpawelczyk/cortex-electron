# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# PowerSync
POWERSYNC_URL=https://your-instance.powersync.journeyapps.com

# AI agent API key (create via app Settings → AI Agents)
CORTEX_API_KEY=ctx_your_key_here

# Agent identity (for task assignment watching)
CORTEX_AGENT_ID=your_agent_uuid

# OpenClaw webhook (for instant task notifications)
OPENCLAW_WEBHOOK_URL=http://127.0.0.1:18789
OPENCLAW_WEBHOOK_TOKEN=your_hook_token

# Daemon
CORTEX_AGENT_ID=           # Agent UUID (for task assignment watching)
CORTEX_AGENT_PORT=7654     # HTTP API port (default: 7654)
CORTEX_WEBHOOK_URL=        # Optional: webhook URL for task notifications
